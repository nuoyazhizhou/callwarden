[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "code-graph-core"
version = "0.1.0"
requires-python = ">=3.10"
description = "Code Graph 高性能 PyO3 扩展（批量余弦相似度）"

[tool.maturin]
features = ["pyo3/extension-module"]
