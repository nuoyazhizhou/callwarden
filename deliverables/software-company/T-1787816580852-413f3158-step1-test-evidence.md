# T-1787816580852-413f3158 step1 test evidence

## 范围

本 step（S-1787816580855-41721ed8, action=test）补充契约验收所需的回归测试：
「进度百分比计算、两位小数展示、历史与新任务回归」。

## 新增测试

`rust_ext/src/daemon/task_collab_tests_projection.rs`：

- `test_task_status_progress_percent_two_decimals`：3-step 任务完成 1/3 → `percent=33.33`，
  完成 2/3 → `percent=66.67`。验证 percent 按 `(ratio*100*100).round()/100` 保留两位小数，
  非整除进度绝不输出 `33.0` / `33.333...` 等错误精度。
- `test_task_status_historical_blocked_vs_bound_projection`：历史任务（仅 tasks 行、无
  workspace binding）返回 `lifecycle_status=in_progress` + `workflow_status=governance_blocked`
  且 blocking_reasons 非空，不按 raw status 猜测；task.create 新任务（有不可变 binding 但
  缺 contract）同样返回 `governance_blocked`（缺可验证治理事实不伪造投影），生命周期保留 `open`。

既有相关测试复核（未修改，回归确认）：

- `test_task_status_includes_steps_and_normalized_progress`（0% / 100% 归一化）
- `test_task_list_includes_daemon_governance_projection`（列表治理投影）
- `task_tree_progress_and_findings_match_contract`（tree 50.00% 两位小数展示）
- `tests/test_cli_093_http_rpc.py::test_cli093_tree_formats_ratio_as_percent`（CLI 758/764 → 99.21%）

## 验证结果

- `cargo test --lib "percent_two_decimals"`：1 passed
- `cargo test --lib "historical_blocked_vs_bound"`：1 passed
- `cargo test --lib "projection::"`：26 passed（含新增 2 个，无回归）
- `cargo test --lib "task_tree_progress_and_findings_match_contract"`：1 passed
- `python -m pytest tests/test_cli_093_http_rpc.py -q -k "ratio or routes_to_daemon"`：2 passed

说明：`test_cli093_tree_task_not_found` / `test_cli093_tree_daemon_unavailable` 两个既有用例
依赖 monkeypatch 的真实 daemon 往返，在本机 daemon 环境下表现为真实调用失败，与本 step
改动无关（未触碰该文件）。

## 结论

step1 验收通过：百分比两位小数、历史/新任务治理投影一致性均有回归测试覆盖并通过。
