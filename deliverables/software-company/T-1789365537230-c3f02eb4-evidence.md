# T-1789365537230-c3f02eb4 执行证据（C-17：admin 路由块 workspace 命名空间权威解析）

- 任务：`T-1789365537230-c3f02eb4`（C-17 承接卡，父 PYT 卡 `T-1788871227327-45c94bd8`）
- 执行身份：`executor-wb-c17-01` / `inst-exec-wb-c17-01` / `sess-exec-wb-c17-20260914`（implementer lease `L-98683177c9bc1072`，fencing=1）
- 修复前部署二进制：`87C6200…`（`runtime/current`，含 C-16 修复，不含 C-17 修复 = 本卡基线）
- 修复后构建二进制：`39806EB8FE82388BC81ECEAA28D52E879BC359767F60363785FEF740352C5920`（`.cw_target_c16/debug/cw-daemon.exe`，CARGO_TARGET_DIR 隔离）

## §1 step0（adjudicate）盘点结论

交付物：`deliverables/software-company/T-1789365537230-c3f02eb4-inventory.md`
（sha256 见 step0 report `req-5f98b547b930`）。要点：

1. 注入点：`snapshot_state.rs:3186-3194` guard（21 方法）/ `:3195-3204` 代理 id / `:3205` `open_write`→`default_codegraph_db_path()`。
2. 方法名计数：guard 列表 = 21，扣卡 A 已收口 2（assignment_create/revoke）= **19 待盘点**；backlog §W18 记 18 的成因 = 按显式 match 串臂计数漏计 `_ =>` 兜底臂的 `admin.select_interface_provider`。
3. 19 handler 分类：4 无风险 / 3 恒失败（FK）/ 8 静默空或误报 / 3 静默误写；不适用工单 §4.3「多数无风险→收窄」分支（15/19 有缺陷），范围维持全部 19。
4. 新发现独立缺陷（非命名空间错配，按 C 桶纪律登记不代修）：
   - **F1** `admin.gc_audit_get/list` 引用不存在的 `tasks.workspace_id` → prepare 失败；
   - **F2** `admin.record_action_identity` 缺 NOT NULL 列 `action_identities.action_id`；
   - **F3** `admin.register_attestation_revocation` 缺 NOT NULL 列 `revocation_id`。
5. 修法裁决：路由层复用同文件既有 `open_codegraph_db_write`（semgrep 写面先例）；step1 target_file 单文件 `snapshot_state.rs`。
6. 修复前实测回执 13 项（对已部署二进制 `87C6200…` 真实 RPC）：见 step0 report。

## §2 step1（implement）路由层修复

- diff：`owned_workspace(...).workspace_id`（registry 代理 ROWID）+ `open_write(self, ws)`
  → 改为 `self.open_codegraph_db_write(peer, ws)`（ACL 不变，真 id 按
  `client_view_root` 规范化匹配 `workspaces.root_path`）。guard 内 19+2 方法一次受益。
- `cargo check --all-targets`（CARGO_TARGET_DIR=.cw_target_c16，隔离）通过，无新增告警。
- report：`req-b1874ee41fcc`。

## §3 step2（test）判别性测试矩阵

新增 `tests/test_c17_admin_route_workspace_authority.py`（18 用例，真实隔离 cw-daemon，
非源码字符串断言）。判别力设计：夹具在隔离 registry 预插 dummy 行占掉 ROWID=1，
真实行 = 2（PROXY_ID），物理库 `workspaces` 仅 id=1 —— 复现生产「代理≠真 id」分裂。

### 3.1 修复后（新二进制 39806EB8…）：**18/18 通过**

```
pytest tests/test_c17_admin_route_workspace_authority.py -q
→ 18 passed in 66.62s
（证据：deliverables/_insp_c17_newbin.txt）
```

### 3.2 反证（基线二进制 87C6200…）：**10 failed / 8 passed**

```
..FFFFFFFFFF......
（证据：deliverables/_insp_c17_baseline.txt）
```

10 条判别性用例全部按 step0 预测失败：

| 类 | 用例 | 基线实况 |
|---|---|---|
| 1 FK 恒失败 | gc_archive_import / record_artifact_identity | `FOREIGN KEY constraint failed` |
| 2 静默空/误报 | gc_archive_inspect/list、gc_retention、snapshot_compare、clear_clones、branch_switch | WHERE 代理 id 不命中 → Null/[]/0/branch_not_found |
| 3 静默脏写 | branch_register、publish_interface、select_interface_provider | 落库 workspace_id=2（代理 id） |

8 条通过恰为与命名空间无关者（ACL×2、gc_policy、audit_rotate_key/cleanup_rule_sync_log、
assignment_create/revoke〔卡 A 内部已走 binding〕、F1-F3 回归锚）。**用例具判别力**。

### 3.3 F1-F3 回归锚（本卡不修，如实失败）

三个已知缺陷用例在修复后仍按预期失败（`no such column` / `NOT NULL constraint failed`），
作为「修复不掩盖已知缺陷」的回归锚；转绿须由独立缺陷卡承接。

### 3.4 Rust 侧回归

`cargo test --lib task_collab`（隔离目标目录）：**150 passed / 0 failed**（16.66s）。

## §4 step3（release_verify）部署与实机验证

### 4.1 提交

- commit `186582ee2f18dddae41500b4d3556eca267d9a0e`（**单 commit**，前缀本卡 task_id）：
  `rust_ext/src/daemon/snapshot_state.rs`（+30/−10）+ `tests/test_c17_admin_route_workspace_authority.py`（+558）。
- `git diff --check` clean。

### 4.2 部署门禁

`scripts/refresh_shared_runtime.ps1 -TaskId T-1789365537230-c3f02eb4`：

```
status=passed / rollback=false / error=null
git_head=186582ee2f18...（== HEAD）
daemon_runtime.sha256=C9C3A5DBA14488F6C81DF59F6A4B4A4157983B756057243F4EB52D84866810D0
ping_exit_code=0
证据 JSON：C:/Users/wanpi/.callwarden/runtime/evidence/20260914-163508-186582ee2f18-bd192b71.json
```

已知环境缺陷复现（不影响部署结果）：脚本 post-swap 清理阶段 `genie-trash`/
`Remove-Item` 对 `versions/*.core-backup` 报拒绝访问（swap 已完成）；脚本以
`Start-Process` 拉起的 daemon（PID 11044）随脚本父进程退出（既有缺陷），已按
卡 C 同款方式以前台 exec 后台常驻重启：**pid 7744 / endpoint http://127.0.0.1:8950 /
git_commit=186582e == HEAD / worker_status=healthy**。
三方哈希一致：`runtime/current/cw-daemon.exe` = `C9C3A5DB…`（== 门禁 JSON `daemon_runtime.sha256`）。

### 4.3 已部署 daemon 只读实测（生产实例 `4baea3ff12c2ea5c`，零副作用）

对**已部署二进制 C9C3A5DB…** 的真实 RPC 回执（`deliverables/_insp_c17_live_out.txt`）：

| 探针 | step0 修复前（对 87C6200…） | 修复后（对 C9C3A5DB…） |
|---|---|---|
| `admin.gc_retention` | archived=0 / active=0（静默空） | **archived_eligible=151 / active_files=2205 / total=2356** |
| `admin.snapshot_compare` | 全 0 | **archived=151 / active=2205 / symbols=39723** |
| `admin.gc_archive_list` | `[]` | **5 行**（id=151 起，rel_path 命中真实归档） |
| `admin.gc_archive_inspect` | `Null` | **命中 id=151**（abs_path 逐字返回） |
| `admin.gc_policy_get` | 默认值（id 无关） | 默认值（不变，符合预期） |
| `admin.record_action_identity`（F2 锚） | NOT NULL 失败 | NOT NULL 失败（不变，已知缺陷如实保留） |

写面（FK 表 / 无 FK 表）的修复效果由 §3 隔离矩阵 + 反证承担（生产不做有副作用探针）。

### 4.4 结论

- 4 个 check item 全部满足：cargo build 零 error；cargo test 150/150 与 HEAD 同集对照零新增失败；
  部署门禁 passed / rollback=false / 三方哈希一致 / health.git_commit==HEAD；diff --check clean；
  commit 前缀为本卡 task_id（`186582e` 单 commit）。
- step0→step3 全链闭环：19 handler 的命名空间错配在路由层一次性收口；F1-F3 按纪律登记不代修。

