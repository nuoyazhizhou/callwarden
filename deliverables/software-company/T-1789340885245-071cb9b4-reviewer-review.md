# T-1789340885245-071cb9b4 —— C-14..C-15 承接卡独立 Reviewer 复核回执（blind_first_pass）

> 本文件是 **reviewer 视角**的 task-bound 独立复核证据，与 implementer 的
> `T-1789340885245-071cb9b4-evidence.md` 相互独立。下列数字均为 reviewer 在本机
> **独立实跑**的回执，**不采信 executor 报告**：编译、回归、五面契约对照、端到端往返
> 均由 reviewer 自行重放后给出 PASS 结论依据。

## 0. 复核身份与 lease

| 项 | 值 |
| --- | --- |
| task_id | `T-1789340885245-071cb9b4` |
| role | reviewer（blind_first_pass） |
| agent_id | `reviewer-c14c15-wb-01` |
| agent_instance_id | `inst-review-c14c15-20260914` |
| session_id | `sess-review-c14c15-20260914` |
| model_id | `workbuddy` |
| reviewer lease | 提交 verdict 时以 `cw lease acquire --role reviewer`（上述 identity）签发；raw token 按协议仅在进程内使用、不落盘，故本回执不固化临时 lease_id（见 §4） |
| 独立于 executor？ | 是（executor 为 `executor-pytreg-01` / `sess-exec-pytreg-01-6c9613cd` / `trae-agent-executor`） |
| 复核绑定的 step | `S-1789340885248-0754adec`（step_index 4，`release_verify`） |

## 1. 复核结论

**overall = pass**，阶段 `blind_first_pass`。

C-14（`handle_assignment_create` 的 INSERT 缺 `assignment_id`，恒 NOT NULL 失败）、
C-15（`handle_assignment_revoke` 入参与五面契约相悖）以及卡内实测新发现的 C-17
（admin 路由传入 registry 代理 `workspace_id`，与 task DB `workspaces.id` 不同命名空间，
令 create 恒 FK 失败 / revoke 恒误报 not_found）均**已在本卡修复且未掩蔽**；未触碰
forbidden_paths（`db/**`、`scripts/refresh_shared_runtime.ps1`、`snapshot_state.rs`、
`task_collab_shared.rs` 均零改动）；回归与端到端往返可在本机复现。

## 2. 独立复算回执（不依赖 executor 报告）

### 2.1 代码复核（只读源码 + `git diff`）

- `rust_ext/src/daemon/admin_handlers.rs`（`1 file changed, 74 insertions(+), 16 deletions(-)`）：
  - 新增 `gen_assignment_id()`：`getrandom::fill(&mut [u8; 8])` + `format!("ASG-{}", hex::encode(entropy))`，
    与 Python `ASG-<uuid4.hex[:16]>` 形态等价，未引入新 crate；
  - `handle_assignment_create`：INSERT 列清单补入 `assignment_id`，返回
    `assignment_id` 字符串（不再 `last_insert_rowid()` 整数）；
  - `handle_assignment_revoke`：入参 `require_str_param(params, "assignment_id")`；
    先 `SELECT task_id FROM task_assignments WHERE assignment_id = ?1`（`.optional()`）反查，
    再 `UPDATE ... WHERE workspace_id = ?2 AND assignment_id = ?3 AND status = 'active'`；
    返回 `{"ok": true, "assignment_id": ..., "revoked_at": ...}`；
  - 两 handler 均 `let _ = workspace_id;` 后改走
    `crate::daemon::task_collab::task_bound_workspace_id(conn, task_id, None)?`。
- 权威 resolver 复核：`task_collab_shared.rs:471-507` 读不可变
  `task_workspace_bindings`，无 binding 时 `E_TASK_WORKSPACE_UNBOUND` fail-closed；
  经 `task_collab.rs` `pub(crate) use` 再导出，故 `crate::daemon::task_collab::` 路径成立。
- schema 复核：`db/schema.py:1626-1639` `assignment_id TEXT NOT NULL UNIQUE`、
  `FOREIGN KEY (workspace_id) REFERENCES workspaces(id)` —— 与修复动机逐字对应。
- 上述四处逻辑与 C-14/C-15/C-17 修复目标一致，未发现掩蔽、绕过或伪造成功。

### 2.2 独立编译（reviewer 自行从源码构建）

```
cargo build --manifest-path rust_ext/Cargo.toml
warning: unreachable pattern                        --> src\bin\cw_cli.rs:2017
warning: function `command_name` is never used      --> src\bin\cw_cli.rs:5418
Finished `dev` profile [unoptimized + debuginfo] target(s) in 0.65s
BUILD_EXIT=0        # 零 error；2 条 warning 均为既有存量，不在本卡文件内
```

### 2.3 独立回归（源码文本提取 + 断言范式，不依赖 live daemon）

```
python -m pytest tests/test_c14_c15_assignment_contract.py tests/test_cli_011_http_rpc.py -q
...................                                                      [100%]
PYTEST_EXIT=0       # 19 passed（15 + 4）
```

### 2.4 五面契约对照（reviewer 逐面只读核验，均无需改动）

| 面 | 位置 | 结论 |
| --- | --- | --- |
| Rust 实现 | `rust_ext/src/daemon/admin_handlers.rs`（本卡 diff） | 入参/返回体/WHERE 子句与下列四面一致 |
| Python 契约 | `db/db_task_leases.py:309-322` `revoke_assignment` | 逐字同源：同一 UPDATE 条件、同一 `{assignment_id, revoked_at}`、同一 not_found 文案 |
| CLI 路由表 | `cli/main.py:1268` | `"revoke_assignment": ("admin.assignment_revoke", "GOVERNANCE_WRITE", ("assignment_id",))` |
| CLI argparse | `cli/main.py:17681-17686`、`:17710` | 位置参数 `assignment_id`；调用 `db.revoke_assignment(opts.assignment_id)` |
| 文档 | `docs/mcp_tools.md:1972`、`docs/cli_reference.md:3251` | `assignment_revoke` 参数 `assignment_id`、返回 `{ok, assignment_id, revoked_at}`；`cw assignment revoke <assignment-id>` |

`docs/**` 无需改动，符合 step#3 的“文档单源一致”验收。

### 2.5 独立 route_matrix 门禁

```
python scripts/verify_route_matrix.py
核对通过（门禁全绿）: names(T)==names(M)==mirror 语义，dispatch 一致，无本地隐式路径
ROUTEMATRIX_EXIT=0
```

### 2.6 独立复现“cargo test 未退化”口径

```
cargo test --manifest-path rust_ext/Cargo.toml --lib -- cli::router::tests cli::runtime::tests cli::refresh::tests
test result: FAILED. 34 passed; 6 failed; 0 ignored; 0 measured; 1744 filtered out
```

reviewer 独立复现出**与 executor 完全相同的 34 passed / 6 failed**，失败用例全部位于
`src/cli/`（router/runtime/refresh 三个子域），与本卡唯一产品落点
`src/daemon/admin_handlers.rs` 不同模块；结合 executor 以 `git stash` 回落 HEAD 的
基线对照（同一 6 例同样失败），判定为**环境既有失败，非本卡引入**。reviewer 未重复
全量 21 分钟 `cargo test`，以同口径子集复现替代，此处如实记录。

### 2.7 端到端往返独立重放（对**已部署** daemon，非仅源码文本）

先独立确认“编译 ≠ 已部署”的规则 43 门禁产物一致性：

```
%USERPROFILE%\.callwarden\runtime\current\cw-daemon.exe   LastWriteTime 14/9/2026 11:40:32  len 45735424
  SHA256 = BE67915CBC5D4641AE3FBC255AC160D21ADA8D791B163CB98B0A0ED19DD3DF92
rust_ext\target\stage-refresh\release\cw-daemon.exe       LastWriteTime 14/9/2026 11:40:32  len 45735424
  SHA256 = BE67915CBC5D4641AE3FBC255AC160D21ADA8D791B163CB98B0A0ED19DD3DF92
```

即**在运行中的共享 daemon 二进制已包含本卡修复**（与 §2.4 部署门禁记录的三方哈希一致）。
reviewer 随即以全新 role/agent 独立重放完整往返（`role=revprobe`）：

| 步 | 命令/通道 | 回执 | 判定 |
| --- | --- | --- | --- |
| 1 create | `cw assignment create T-... --role revprobe --agent-id reviewer-c14c15-wb-01 ... --json` | `{"ok":true,"assignment_id":"ASG-d32d37a9c0bdec45","task_id":"T-1789340885245-071cb9b4","role":"revprobe",...,"created_at":1789362378.28}` | ✅ C-14 已修（无 NOT NULL / 无 FK 错误），id 形态 `ASG-<16hex>` |
| 2 show | RPC `assignment_show`，显式 `workspace_id=1` | `{'id': 449, 'workspace_id': 1, 'assignment_id': 'ASG-d32d37a9c0bdec45', ..., 'status': 'active', 'revoked_at': None}` | ✅ positive 分支命中，与步 1 逐字一致 |
| 3 revoke | `cw assignment revoke ASG-d32d37a9c0bdec45 --json` | `{"ok":true,"assignment_id":"ASG-d32d37a9c0bdec45","revoked_at":1789362390.91}` | ✅ 契约 D4 |
| 4 show（撤销后） | 同步 2 通道 | `{'status': 'none', ...}` | ✅ append 语义生效 |
| 5 revoke 重放 | `cw assignment revoke ASG-d32d37a9c0bdec45 --json` | `assignment_not_found: assignment_id=ASG-d32d37a9c0bdec45 不存在或已撤销`（RC=2） | ✅ 契约 D5，幂等拒绝 |

另外独立复核了 executor 声明的 C-16 现象：**生产调用方形态**（不带显式 `workspace_id`）
下 `assignment_show` 返回 `{'status': 'none', ...}`，与 §4.1 出界披露一致（见 §3）。

### 2.8 证据文件哈希复核

implementer evidence manifest
`deliverables/software-company/T-1789340885245-071cb9b4-evidence.md`
SHA-256 = `2BF673EB8227A28FBCD9BE4744E2CF5BB0A60831A5AF3C9C13F04478972B6ED8`，
与 daemon `task_events` 中 step#4 report（`request_id=req-6b9314ce3dff`、
`step_id=S-1789340885248-0754adec`）持久化的 `evidence_hash`
**逐字一致**（reviewer 直接从 `task_events` 读出对照，未采信聊天陈述）。

### 2.9 工作树与边界

`git status --short` 只有两处产品/测试改动与新增证据文件：
`M rust_ext/src/daemon/admin_handlers.rs`、`M tests/test_cli_011_http_rpc.py`、
`?? tests/test_c14_c15_assignment_contract.py`、`?? deliverables/software-company/*`。
`git diff --check` → exit 0。`db/**`、`scripts/**`、`snapshot_state.rs`、
`task_collab_shared.rs` 零改动，未越界。

## 3. 残余与如实披露（不隐瞒）

- **C-16（出界，另建卡）**：`assignment_show` 的 positive 分支对生产调用方不可达
  （CLI/MCP 侧不给数值 `workspace_id`），reviewer 已独立复现。根因落在
  `task_collab_lease.rs` 与 `server/daemon_client.py`，**均不在本卡 allowed_paths**，
  故步 2/4 按 step0 裁决 D10 走 daemon RPC 层显式 `workspace_id` 取证，未伪装
  CLI 端到端。
- **未新增隔离 daemon live pytest**：共享夹具 `_pick_bin()` 优先解析
  `rust_ext/target/release/cw-daemon.exe`（该文件早于本卡修复），直接复用会得到
  环境失真的失败；改动共享夹具会波及全套收敛测试，超出 `executor_allowed`。
  本卡以“源码文本确定性回归（19 例）+ 规则 43 部署门禁哈希一致 + §2.7 对已部署
  daemon 的独立往返重放”替代，口径已写入 evidence §4.5。reviewer 认为该替代
  **足以支撑 PASS**：往返证据来自**在跑的、哈希已证实含修复的** daemon，而非文本推断。
- **lease 归属**：本 reviewer 身份 `reviewer-c14c15-wb-01` 未走 `agent.register`
  （`agent.register` 属 `PROTECTED_MUTATION_METHODS`，需治理 lease 凭证，不在本次
  reviewer 会话的可授权面内），与 C-13 卡先例一致；此处如实披露，未伪造注册状态。
- **C-17 剩余暴露面**：`snapshot_state.rs` admin 路由块对其余 18 个 admin handler
  存在同类“registry 代理 id 当 task DB workspace_id”的命名空间风险，本卡仅收口
  assignment 两处，其余**未改、未评估**（evidence §4.2 已登记，另卡承接）。

## 4. verdict provenance

- task_contract：`TC-T-1789340885245-071cb9b4` revision=1
  hash=`sha256:8050e0816d872eac4a1c3dc8e7bd8870460adb11bd7edb6e2f2ba4f9072ba500`
- role_contract：`rcl-T-1789340885245-071cb9b4-reviewer` revision=1
  hash=`sha256:3f691024970453f94a17d7ddc7a5845db2eaf19f1d25af9b8c9000272a6f8863`
- review_input_snapshot_id：`dfcac6f16b827a30`
- view_manifest_hash：`86ad4321954e9999e0cebe58962bf777edd7fbd85b5c0af03a9263ad114ba0c5`
- 提交 phase=`blind_first_pass`，overall=`pass`，findings=0，clause_results=[]
- reviewer lease：提交时由上述 identity 以 `--role reviewer` 现场 acquire；raw token 仅在
  同一进程内使用，未写入本回执 / evidence / ledger / 日志（符合 role-protocol §7）。
  首个尝试用 lease 因 raw token 未落盘且 holder 未走 `agent.register`，被 daemon 权威
  orphan 回收（`holder_registration_missing`），随后由同一 identity 重新 acquire 提交，
  counter 递增；未绕过门禁、未伪造 token、未直接写库。

## 5. 结论与后续

Reviewer 判 **PASS**。依据 role-protocol §7，提交 `reviewer_pass` handoff 将任务移交
adjudicator 进行受保护收尾（apply → `applied_pending_close` → close → `completed`）。
