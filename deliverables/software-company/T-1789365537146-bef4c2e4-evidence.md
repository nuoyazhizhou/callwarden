# C-16 承接卡执行证据（卡 C · `T-1789365537146-bef4c2e4`）

- **卡**：`T-1789365537146-bef4c2e4`（C-16 承接：daemon assignment_show workspace 权威解析修复）
- **父卡**：`T-1788871227327-45c94bd8`（PYT 回归卡）
- **仓库 / 分支**：`C:/git_work/callwarden` / `master`
- **基线 HEAD（开工时）**：`b2056b48eb20f122303c628c31049fb6422097b1`
- **执行身份**：`executor-wb-c16-01` / `inst-exec-wb-c16-01` / `sess-exec-wb-c16-20260914`
- **daemon 端点**：`http://127.0.0.1:11338`（`cw daemon health` 回执；交接文档 §7.1 记的 1615 已下线）
- **snapshot**：`dfcac6f16b827a30`（`cw collab publish --workspace=C:/git_work/callwarden` 发布，instance `4baea3ff12c2ea5c`）
- **证据文件性质**：累进证据（每步 report 后本文件 sha256 变化，report 引用当期 hash）

---

## 1. step0（adjudicate）· 契约裁决

- step_id：`S-1789365537156-bf8ea184`
- report_request_id：`req-3b81da3151a6`
- 独立交付物：[T-1789365537146-bef4c2e4-contract-adjudication.md](file:///c:/git_work/callwarden/deliverables/software-company/T-1789365537146-bef4c2e4-contract-adjudication.md)
  （sha256 `aed36c140a9e5bc1013cdfba77caf098195f474dd335a268d761aad96d977d18`，13181 bytes）

**裁决结论（三条）**：

1. **daemon 侧缺省路径属实未变** — `handle_assignment_show` 原以
   `unwrap_or(0)` 取 `workspace_id`（`task_collab_lease.rs:2008-2011`），与
   `task_assignments.workspace_id` 的实际取值域（`{1,2,3,5,6,7,8,9,10,11,17,19,20,21}`，本机实测）
   不同源 → SQL 恒空 → 恒走 `none` 分支。
2. **调用侧有两个独立注入门禁（对交接文档 §3.2 的更正更正）** —
   `server/daemon_client.py`：门禁 A（`:3953-3957`）按**通用**
   `_is_task_scoped_authority_request()`（`:3530-3551`，非空 `task_id`/`superseded_id`）
   跳过 **instance/root** 注入块；门禁 B（`:3973-3977`）**只对 `rpc_method` 以
   `task.`/`lease.` 开头者**注入**数值** `workspace_id`。`assignment_show`
   （CLI 映射 `cli/main.py:1159`）**同时被两个门禁拦下**。
   → 交接文档 §3.2 把卡 A 证据 §4.1「只对 `task.`/`lease.` 前缀注入」判为**陈旧表述，
   该判断不成立**：前缀门禁在本 HEAD 确实存在（`:3974`），且对 `assignment_show` 而言
   它才是数值 `workspace_id` 缺失的**直接原因**；门禁 A 跳的是 instance/root 注入块。
   **两份文档各自描述一个真实门禁，都不完整，也都不是「陈旧」。**
3. **`server/daemon_client.py` 裁决为不改** — 契约单源（`:3531-3545` docstring 声明
   task-scoped 应由 daemon 从 `task_workspace_bindings` 解析）；改门禁会注入 legacy
   active workspace（多项目下可能属别的项目）且 blast radius 覆盖全部 `task.*`/`lease.*`。
   本卡范围收敛为 **daemon 单侧修复**。

**live「修复前」对照（同数据、三通道、结论分歧）**：活体夹具 `task_assignments.id=450`
（`A-8efb4d6a242692d9031d2349`，role `implementer`，status `active`，**本卡 claim 时 daemon 自建**）

| 通道 | 参数 | 结果 |
|---|---|---|
| CLI | `cw assignment show T-1789365537146-bef4c2e4 --role implementer` | `Active Assignment / assignment_id: None` |
| 裸 RPC | `{task_id, role}`（复刻 CLI 线上参数） | `{"status":"none", task_id, role}` |
| 裸 RPC | `{task_id, role, workspace_id: 1}` | 命中 `id=450` / `A-8efb4d6a242692d9031d2349` |

---

## 2. step1（implement）· `handle_assignment_show` 走权威 resolver

- step_id：`S-1789365537156-bf903954`
- 落点文件：`rust_ext/src/daemon/task_collab_lease.rs`（**唯一改动文件**）
- 文件 sha256（after）：`f43bca23e13d85aaa48f4d46258c3ec74814b585c067fbb4e5b022d53145e20c`

### 2.1 改动内容

```diff
--- a/rust_ext/src/daemon/task_collab_lease.rs
+++ b/rust_ext/src/daemon/task_collab_lease.rs
@@ -2000,15 +2000,18 @@ impl TaskCollabStore {
 // 语义与 Python db_task_leases.get_assignment 一致：查 task_assignments
 // （workspace_id + task_id + status='active'，role 非空时追加过滤），
 // ORDER BY id DESC LIMIT 1；无命中返回 {"status": "none", task_id, role}。
+//
+// workspace authority（C-16）：数值 `workspace_id` 只来自不可变
+// `task_workspace_bindings`，由 `task_bound_workspace_id` 解析。调用侧
+// `route_rpc` 对 task-scoped 请求刻意不注入 workspace（门禁 A 跳过
+// instance/root，门禁 B 只对 `task.`/`lease.` 前缀注入数值 id），因此本 handler
+// 必须自己解析；旧实现回落到常量 0 与 binding 命名空间不同源，恒不命中
+// → 恒返 none，使 positive 分支对 CLI/MCP 调用方整体不可达。
 pub fn handle_assignment_show(
     &self,
     _peer: PeerCredential,
     params: &Value,
 ) -> Result<Value, DaemonRpcError> {
-    let workspace_id: i64 = params
-        .get("workspace_id")
-        .and_then(|v| v.as_i64())
-        .unwrap_or(0);
     let task_id = params
         .get("task_id")
         .and_then(|v| v.as_str())
@@ -2020,7 +2023,20 @@ pub fn handle_assignment_show(
         .unwrap_or("")
         .to_string();
 
+    // C-16：task_id 必填（CLI 恒传）。空 task_id 无从解析 binding，必须在取 conn
+    // 之前 fail-closed，禁止带着空 task_id 去查 binding。
+    if task_id.is_empty() {
+        return Err(DaemonRpcError::invalid_params(
+            "assignment_show 需要非空 task_id（workspace 由不可变 task_workspace_bindings 解析）",
+        ));
+    }
+
     let conn = self.conn.lock().unwrap();
+    // C-16：缺省 workspace 由不可变 binding 解析；显式传入则由 resolver 做一致性
+    // 校验（无 binding → E_TASK_WORKSPACE_UNBOUND；显式不一致 →
+    // E_WORKSPACE_AUTHORITY_MISMATCH）。绝不回落到 legacy active workspace 或常量。
+    let workspace_id =
+        task_bound_workspace_id(&conn, &task_id, optional_workspace_id_param(params))?;
     let map_row = |r: &rusqlite::Row| -> rusqlite::Result<Value> {
```

### 2.2 逐条对照 step 的 check items

| check item | 落实 |
|---|---|
| 缺省 workspace 走 `task_bound_workspace_id` | ✅ `let workspace_id = task_bound_workspace_id(&conn, &task_id, optional_workspace_id_param(params))?;` |
| `requested` 取自参数、缺省由 binding 解析 | ✅ `optional_workspace_id_param`（`task_collab_shared.rs:455-463`）：`None`=未提供、筛除 `<= 0`、兼容字符串数值 |
| `task_id` 为空 → 取 conn 前 fail-closed `invalid_params` | ✅ 空检查位于 `self.conn.lock()` **之前**，直接 `return Err` |
| 无 binding → `E_TASK_WORKSPACE_UNBOUND` | ✅ 由 resolver 提供（`task_collab_shared.rs:486-494`），不再静默回 `none` |
| 显式不一致 → `E_WORKSPACE_AUTHORITY_MISMATCH` | ✅ 由 resolver 提供（`:495-505`） |
| 保持只读语义、不改 `db/**`、不直写 SQLite | ✅ 仅改取值来源；SQL、`ORDER BY id DESC LIMIT 1`、`status='active'`、`none` 返回体**逐字未变**；未触碰 `db/**` |

- 采用**同文件既有范式**（`task_collab_lease.rs:1906-1912`，`handle_lease_list_events`），
  **未新增 helper、未新增 import**（`use super::*;` 已覆盖）。
- `_peer` 仍未使用 —— 本卡**不动**（是否加身份校验属另一 finding，不在 C-16 scope）。

### 2.3 构建回执

```
$ source scripts/msvc-env.sh            # INCLUDE(VS2022) 保留，CARGO_INCREMENTAL=0
$ CARGO_TARGET_DIR=C:/git_work/callwarden/.cw_target_c16 cargo check --all-targets
  Finished `dev` profile ... in 5m 09s          RC=0     # 仅既有 warning（159，无新增 error）
$ CARGO_TARGET_DIR=C:/git_work/callwarden/.cw_target_c16 cargo build --bin cw-daemon
  Finished `dev` profile ... in 5m 42s          RC=0     # 仅既有 warning（180）
```

- 隔离 target dir（`.cw_target_c16`，未跟踪），**未**在活 daemon 使用的
  `rust_ext/target/release` 上构建，避免链接锁冲突。

### 2.4 本步未做

- **未部署 runtime**（属 step3；`scripts/refresh_shared_runtime.ps1` 未运行）。
- **未跑 cargo test**（属 step3 回归对照）。
- **未改 `server/daemon_client.py`**（step0 裁决：不改）。

---

## 3. step2（test）· 负向矩阵功能验证

- step_id：`S-1789365537156-bf909a98`
- 新增文件：`tests/test_c16_assignment_show_workspace_authority.py`
  （sha256 `cf684c39ce3494d3d787341d44af58b8853d2259170b198626bdb539dcbc1a13`）

### 3.1 为什么不是源码契约测试

卡 A 的 `tests/test_c14_c15_assignment_contract.py` 是「文本提取 + 字符串断言」范式；
本卡 step 的 check item 明确要求**不得只做源码字符串断言充数**。故本套件启动
**真实 `cw-daemon` 进程**（隔离临时数据目录 / task DB / USERPROFILE，HTTP 绑临时端口），
对矩阵逐条做真实 RPC 往返，断言响应体与隔离库落库结果。

夹具要点：
- 隔离 task DB 用**仓库权威 schema**（`callwarden.db.schema.SCHEMA_TABLES_SQL` /
  `SCHEMA_INDEXES_SQL`）建表（**不手写 DDL**，避免与生产 schema 漂移——`task_assignments.workspace_id`
  的 FK 目标 `workspaces(id)` 正是 C-16 的命名空间来源）；
- daemon 侧不对隔离库做迁移，故夹具在**启动 daemon 之前**建库并种入
  `workspaces(id=1)` / `task_workspace_bindings` / 1 条 active + 1 条 revoked assignment；
- 二进制选择顺序：`CW_DAEMON_BIN` → `rust_ext/target/debug` → `stage-refresh/release`
  → `target/release` → `runtime/current`；夹具把**实际选中的路径 + SHA256** 打印到 stdout，
  供证据引用（避免「用了哪个 binary」不可考）。

### 3.2 用例清单（10 例，覆盖合同 6 条矩阵）

| 用例 | 对应矩阵 | 断言 |
|---|---|---|
| `test_matrix_1_*` | ① | 无 `workspace_id` + 有 binding → `assignment_id` 逐字命中、`status=active` |
| `test_matrix_1b_*` | ① 变体 | 不带 `role` 过滤同样命中（覆盖 SQL 的 else 分支） |
| `test_matrix_2_*` | ② | 无 binding → `E_TASK_WORKSPACE_UNBOUND`，**且 result 为 None（不得静默 none）** |
| `test_matrix_3_*` | ③ | 显式不一致 `workspace_id` → `E_WORKSPACE_AUTHORITY_MISMATCH` |
| `test_matrix_3b_*` | ③ 变体 | 显式**一致**值 → 放行（覆盖 resolver 双分支） |
| `test_matrix_4_*`（3 参数） | ④ | `{}` / `""` / `"   "` → `invalid_params` |
| `test_matrix_5_*` | ⑤ | 已 revoked（role 隔离）→ `{"status":"none"}` |
| `test_matrix_6_*` | ⑥ | 全部调用前后 `task_assignments` 全量行集合与内容**逐字不变** |

### 3.3 运行回执

**A/B 反证（同一套用例、两个 binary）** —— 证明用例不是空转：

```
## BEFORE：旧部署 binary（未含本卡修复）
$ CW_DAEMON_BIN=C:/Users/wanpi/.callwarden/runtime/current/cw-daemon.exe \
    python -m pytest tests/test_c16_assignment_show_workspace_authority.py -q
  [C-16 harness] bin_sha256=BE67915CBC5D4641AE3FBC255AC160D21ADA8D791B163CB98B0A0ED19DD3DF92
  → 失败 4 组：
    · ① AssertionError: positive 分支对生产调用方不可达（C-16 缺陷回归）: {'status': 'none', ...}
    · ② AssertionError: 无 binding 必须 fail-closed，实际返回: {'status': 'none', ...}
    · ③ AssertionError: 不一致必须拒绝，实际返回: {'status': 'none', ...}
    · ④ AssertionError: 空 task_id 必须 fail-closed，实际返回: {'status': 'none', 'task_id': '', 'role': ''}

## AFTER：本卡新构建 binary（含修复）
$ CW_DAEMON_BIN=C:/git_work/callwarden/.cw_target_c16/debug/cw-daemon.exe \
    python -m pytest tests/test_c16_assignment_show_workspace_authority.py -v -s
  [C-16 harness] bin_sha256=37F4182A4137A025768EA1BED9B54C005229D992108BA22FC3223D3488F96D42
  [C-16 harness] seeded=[(1, 1, 'ASG-c16active…', 'T-C16-BOUND', 'implementer', …, 'active', …),
                         (2, 1, 'ASG-c16revoked…', 'T-C16-BOUND', 'c16revprobe', …, 'revoked', …)]
  ============================= 10 passed in 9.64s =============================
```

旧 binary 上 ②③ **返回 `{"status":"none"}`** —— 正是「静默掩盖」而非 fail-closed，反证用例有效。

### 3.4 ⚠️ 范围偏差自报（供 Reviewer 判定）

本步在**首次运行**矩阵时发现实现缺陷，并在**本步内**做了修复：

- **现象**：`task_id="   "`（纯空白）未被判空 → 仍进入 resolver 查询 → 报
  `E_TASK_WORKSPACE_UNBOUND` 而非 `invalid_params`。首轮结果为 **9 passed / 1 failed**。
- **判定**：这违反 step1 check item 的原文「task_id 为空时必须在取 conn 之前 fail-closed
  **（不得带着空 task_id 去查 binding）**」——空白 task_id 同为不可用于解析 binding 的无效值，
  且与调用侧 `_is_task_scoped_authority_request` 的 `value.strip()` 口径不一致。因此属于
  **同一 handler、同一 check item 范围内的边界补齐**，非新增需求。
- **修法（一行）**：`task_id` 提取后加 `.trim()`：
  ```diff
     let task_id = params
         .get("task_id")
         .and_then(|v| v.as_str())
         .unwrap_or("")
  +      .trim()
         .to_string();
  ```
- **范围偏差（显式披露）**：该改动落在 `rust_ext/src/daemon/task_collab_lease.rs`，
  **不在本步 step 的 `target_file`（`tests/test_c16_assignment_show_workspace_authority.py`）白名单内**，
  故 `changes` 无法机器化登记该文件。此处按诚实披露要求显式登记，并给出可核验哈希：
  - `rust_ext/src/daemon/task_collab_lease.rs` 最终 sha256 =
    `14e1d280f68741230bedba1fb0fce565558c414d02ffd6e680e9bf0454226d3a`
    （step1 report 时为 `f43bca23e13d85aaa48f4d46258c3ec74814b585c067fbb4e5b022d53145e20c`）
  - step3 的 `release_verify` 将以该最终哈希 + 完整 diff 重新取证。
  - **请 Reviewer 明确判定该偏差是否可接受**；若判不可接受，请以 `reviewer_blocked` 退回，
    由 `fix_defect` 路径重走（届时可把该文件纳入 step 白名单）。

### 3.5 本步未做

- **未部署 runtime**（属 step3）。
- **未跑 cargo test 全量**（属 step3）。
- 未新增 live「生产 runtime」用例（隔离 daemon 用例已覆盖矩阵；生产 runtime 端到端在 step3）。

---

## 4. step3（release_verify）· 部署门禁 + 实机往返 + 回归对照

- step_id：`S-1789365537156-bf90f8bc`
- 落点：`rust_ext`（release_verify，`changes=[]`；本卡真实变更已在 step1/step2 登记于 `change_audit`）
- 最终源码哈希：`rust_ext/src/daemon/task_collab_lease.rs` = `14e1d280f68741230bedba1fb0fce565558c414d02ffd6e680e9bf0454226d3a`
  （含 §3.4 的 `.trim()` 补齐；step1 report 期哈希为 `f43bca23…`，差异已在 §3.4 显式披露）

### 4.1 部署门禁（三方哈希一致）

- 命令：`scripts/refresh_shared_runtime.ps1 -TaskId T-1789365537146-bef4c2e4`
- 回执：`REFRESH_RC=0` / `status=passed` / `rollback=false`
- 部署后 daemon（`cw daemon health`）：
  `endpoint=http://127.0.0.1:2518`、`pid=33444`、`git_commit=b2056b48eb20f122303c628c31049fb6422097b1`（== 开工 HEAD）、
  `worker_status=healthy`、`schema_version=60`
- **三方哈希一致**：
  `stage-refresh/release` == `runtime/current` == 运行进程
  = `87C6200950A91713F74B5F49E9239E685F5B8CB231D8FD9A446F523EEC4250FE`

> ⚠️ **溯源披露（诚实登记）**：`refresh_shared_runtime.ps1:537` 仅以 `git rev-parse HEAD` 打戳，**不校验工作树是否脏**。
> 本次部署时 C-16 修复**尚未提交**，故该二进制**内容含修复**，却被打上 `b2056b4`（该提交不含修复）——
> 字符串与语义不一致。已在 §4.4 通过「先提交、再重跑门禁」消除。

### 4.2 实机 CLI 往返（show → create → revoke → show）

探针 `deliverables/_insp_c16_live_roundtrip.py`（**只读 + 一次自建 probe 后撤销**，无残留 active 行），
输出 `deliverables/_insp_c16_live_roundtrip_out.json`，9 步全绿：

| 步骤 | 命令 | 结果 |
|---|---|---|
| 1 | `assignment show <T> --role implementer` | **返回 active 行 `id=456` / `A-1d2a310811f590b1466afcf9`**（修复前恒为 `none`）← C-16 缺陷已修 |
| 2 | `assignment create <T> --role c16probe …` | `ASG-bd88bcb9c2e4b967` |
| 3 | `assignment show <T> --role c16probe` | active（命中刚建行） |
| 4 | `assignment show <T> --role implementer` | 仍 active（probe 不污染他角色） |
| 5 | `assignment revoke ASG-bd88bcb9c2e4b967` | ok |
| 6 | `assignment show <T> --role c16probe` | `{"status":"none"}`（追加/撤销语义正确） |
| 7 | `assignment show <T> --role implementer` | 仍 active |
| 8 | `assignment show T-c16-unbound-probe-0001`（无 binding） | RC=2 `E_TASK_WORKSPACE_UNBOUND` **fail-closed** ✓ |
| 9 | `assignment show "   "`（纯空白 task_id） | RC=2 `invalid_params` ✓ |

### 4.3 回归对照（同集 + 二进制身份自证）

`check_item` 要求「零新增失败，须与 HEAD 基线同集对照」。同集 = 
`tests/test_mcp_assignment_show_http_rpc.py` + `tests/test_c14_c15_assignment_contract.py`（共 20 例）。

| 侧 | 二进制 sha256 | 结果 |
|---|---|---|
| **基线（C-16 前）** | `BE67915CBC5D4641AE3FBC255AC160D21ADA8D791B163CB98B0A0ED19DD3DF92`（`runtime/previous-20260914-143641`） | **20 passed** |
| **本卡（C-16）** | `87C6200950A91713F74B5F49E9239E685F5B8CB231D8FD9A446F523EEC4250FE`（stage-refresh） | **4 failed / 16 passed** |

**新增失败 4 例**（统一签名 `E_TASK_WORKSPACE_UNBOUND: task=X 未绑定不可变 workspace（task_workspace_bindings 缺失），拒绝操作`）：

- `test_assignment_show_no_match`
- `test_assignment_show_with_role`
- `test_assignment_show_unknown_workspace`
- `test_assignment_show_new_client_instance_stable`

**二进制身份自证**：`tests/_w3_harness.py::find_daemon_binary()` 取全部候选中 **mtime 最新**者，
故探针先打印实际选中路径 + sha256 再执行（见 `_insp_c16_baseline.txt` / `_insp_c16_newbin.txt` 的
`exists=True / sha256=…` 自检行）。

> ⚠️ **基建坑（同时登记为隐患）**：`find_daemon_binary()` 把 `CW_DAEMON_BIN` 只当作**候选中按 mtime 竞争的一项**
> （`:600`），且 `os.path.isfile()` 对 **MSYS 风格路径**（`/c/Users/...`）在 Windows Python 下判 False →
> 该候选被**静默跳过**，回落到仓库内 **陈旧的 `rust_ext/target/debug/cw-daemon.exe`**（本机为 09-14 12:56 的
> `F1898508…`，早于本卡修复）。本次实验已复现一次假阳性（`CW_DAEMON_BIN=/c/…` 时「20 passed」，
> 实为测了陈旧 debug 构建）。后续所有结论均改用**原生 `C:/…` 路径**并附身份自检。

### 4.4 Rust `cargo test`

- `cargo check --all-targets` → RC=0（step1，`.cw_target_c16`；仅既有 warning，无新增 error）。
- `cargo test`：
  - 首次以默认 target dir（`rust_ext/target`）运行**被沙箱拦截**（拒绝写 `rust_ext/target/debug/**`、
    拒绝执行 `link.exe`；报 `LNK1104` / `LNK1201` / `os error 5` / `拒绝访问`），**属环境权限而非代码问题**。
  - 改用沙箱允许的隔离 `CARGO_TARGET_DIR=.cw_target_c16` 聚焦运行：
    `cargo test --manifest-path rust_ext/Cargo.toml --lib task_collab`
    → **`test result: ok. 150 passed; 0 failed; 0 ignored; 0 measured; 1634 filtered out; finished in 23.77s`，RC=0**
    （`daemon::task_collab::tests::*` 全绿，含 `projection` / `ws_authority` / `verdict_compat` 等治理用例）
- **Rust 侧无覆盖 `assignment_show` 的用例**：`grep -rn "assignment_show" rust_ext/src/**/*.rs` 命中的
  全部是 `dispatch.rs` 路由表、`http_server.rs` 白名单、`route_matrix.rs` 条目与 handler 本体，
  **无 `#[test]` 调用** → 本卡改动不触达任一 Rust 单测断言（150/150 与基线一致）。

### 4.5 ⚠️ 相邻缺陷（adjacent defect，超出本卡 scope，建议承接卡 C-18 / backlog §W19）

§4.3 的 4 例新增失败并非本卡代码缺陷，而是 **MCP-015（`T-1788963088148-495d7208`）测试文件里的陈旧断言**：
`tests/test_mcp_assignment_show_http_rpc.py` 以合成 task_id（`X` / `NO-SUCH-TASK` / `999999`）断言
`assignment_show` 在「无 active assignment」时返回 `{"status":"none"}`——这编码的是**权威模型落地之前**的
「静默 none」语义。C-16 让 `handle_assignment_show` 走权威 resolver 后，无 binding 的 task **必须**
fail-closed（`E_TASK_WORKSPACE_UNBOUND`），二者不可能同时成立。

判定为**陈旧断言**的依据：

1. **兄弟 handler 全部 fail-closed**：`handle_lease_acquire`（`task_collab_lease.rs:1258`）、
   `task.apply/close`、`task.report`、`task.supersede` 等一律 `task_bound_workspace_id`；
   `task_workspace_id_or_active` 仅 1 处（provenance helper `task_collab_shared.rs:170`），不在任何 handler 门禁上。
2. **resolver 自述即 fail-closed**：`task_collab_shared.rs:465-470` 明确「绝不回退 active workspace 或客户端 numeric id」。
3. **本卡合同即 fail-closed**：step0 裁决（§1）+ step1 check items 均要求无 binding → `E_TASK_WORKSPACE_UNBOUND`。
4. 该文件用 `w3_live` harness，而 `setup_w3_client` **只 seed `workspaces`、不 seed `task_workspace_bindings`**
   （`tests/_w3_harness.py:697-708`）→ 该 harness 内所有 task 天然 unbound。

**处置（守 scope 纪律）**：本卡 **不改** `tests/test_mcp_assignment_show_http_rpc.py`，也**不改**共享
`_w3_harness.py`：它们属另一张卡的产物与全 W3 家族共享件，改动的期望语义（种 binding 以保留「no-match→none」覆盖）
须由承接卡连同回归面一起定界。已登记 backlog §W19，建议承接卡 C-18。

### 4.6 提交与提交后重部署（消除 §4.1 溯源缺口）

- **提交**：`5cfb158` — `[T-1789365537146-bef4c2e4] fix(rust_ext): assignment_show 走权威 workspace resolver（C-16）`
  （2 files changed, 360 insertions(+), 4 deletions(-)；前缀为本卡 task_id，未复用 PYT 卡或卡 A）
  - 白名单暂存：`rust_ext/src/daemon/task_collab_lease.rs` + `tests/test_c16_assignment_show_workspace_authority.py`
    （**未** `git add .`；`deliverables/_insp_*.py`、`.cw_target_c16/`、`_c13_impl_*.diff` 一律未入库）
- **重跑部署门禁**：`REFRESH_RC=1`，但失败点在**切换之后的清理步骤**
  （`[safe-delete][SAFE_DELETE_FAIL_CLOSED] ... genie-trash failed; refusing fallback delete`——
  **已知环境缺陷**，非部署失败）。门禁证据 JSON（`runtime/evidence/20260914-150702-5cfb1589f6df-e53fee93.json`）实测：

  | 字段 | 值 |
  |---|---|
  | `status` / `rollback` / `error` | `passed` / `false` / `null` |
  | `git_head` | **`5cfb1589f6df5a3844f8d515ae3f3bc49415e030`**（= 含修复的提交） |
  | `daemon_runtime.sha256` == `expected_sha256` | `87c6200950a91713f74b5f49e9239e685f5b8cb231d8fd9a446f523eec4250fe` |
  | `daemon_runtime.ping_exit_code` | `0` |
  | `daemon_start_action` / `daemon_start_pid` | `start` / `36756` |

  → **可复现性证据**：同一提交源码重建出的二进制哈希与 §4.1 首次部署**逐字相同**（`87C6200…`）。

- **门禁脚本退出后 daemon 随进程树退出**（Windows 子进程收容），已按既有范式以
  「后台前台 exec」重新拉起：daemon `pid=5236`、`endpoint=http://127.0.0.1:8950`、
  **`git_commit=5cfb1589f6df…`（== HEAD，语义正确）**、`worker_status=healthy`。
- **提交后重跑实机往返**（`_insp_c16_live_roundtrip_out2.json`）：三方哈希
  `runtime/current == stage-refresh == 87C6200…`；step1 `show(implementer)` 仍命中 `id=456`、
  step5 revoke ok、step6 `none`、step7 仍 active、step8 `E_TASK_WORKSPACE_UNBOUND`、
  step9 `invalid_params`——**与 §4.2 逐字一致**。

### 4.7 本步结论

- ✅ 部署门禁 `status=passed` / `rollback=false`；三方哈希一致；`git_commit` == 含修复的 HEAD。
- ✅ 实机 CLI 9 步往返全绿（提交前、提交后各一次，结果一致）。
- ✅ `git diff --check` clean；`cargo check --all-targets` RC=0；`cargo test --lib task_collab` 150/150。
- ✅ 提交前缀为本卡 task_id `[T-1789365537146-bef4c2e4]`。
- ⚠️ **唯一未达标项**：`check_item 1` 的「零新增失败」在 **Python 同集对照**上不成立——
  4 例新增失败，全部为 §4.5 的**陈旧断言**（属 MCP-015 测试文件，超本卡 scope，已登记 backlog §W19，
  建议承接卡 C-18）。**请 Reviewer 就该偏差做出判定**。

