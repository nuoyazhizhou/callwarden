# T-1787850432491-f42a2b8c — test split and regression evidence

## Task binding

- task_id: `T-1787850432491-f42a2b8c`
- step_id: `S-1787850432491-f433c5c0`
- scope: `rust_ext/src/daemon/` test modules and line-count gate
- role: executor

## Test extraction

The original embedded `task_collab` test module was split into:

- `task_collab_tests.rs` — test module entry
- `task_collab_test_support.rs` — shared fixtures and role-worker helpers
- `task_collab_tests_core.rs`
- `task_collab_tests_lease.rs`
- `task_collab_tests_governance.rs`
- `task_collab_tests_projection.rs`

The production root now has 1859 lines. Every code file introduced or changed by
this split is below 2000 lines.

## Verification

- `tokenslim run cargo check`: passed (exit code 0).
- `tokenslim run cargo test task_collab::tests::`: **89 executed, 86 passed, 3 failed**.

Failed tests:

1. `test_task_collab_full_lifecycle`: expected 3 events, observed 7. The current
   task creation path emits additional governance/binding events.
2. `test_task_collab_migrates_v46_db_to_v50`: expected 1 event, observed 2. The
   current migration/create path emits an additional authority event.
3. `test_orphan_claim_recovery_requires_stale_owner_and_preserves_step_state`:
   after recovery reports `released`, a new Executor claim still returns
   `task_conflict`.

The first two are stale count assertions relative to the current governance event
model. The third is a behavioral stale-claim recovery defect and must not be hidden
by changing the expected result during a pure refactor.

## Environment limitation

The required database refresh remains unavailable: `python C:/git_work/callwarden/cw.py
--refresh-all` returns daemon `method_not_found: build_full_graph`, while targeted
evidence refresh returns `FOREIGN KEY constraint failed`. No SQLite fallback was used.
