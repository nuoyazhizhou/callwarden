# 承接卡证据：T-1789290073049-6442e268（C-04..C-07 + 追加 C-10..C-12）

- 卡标题：C-04..C-07 承接：`cli/main.py` i18n 遮蔽与 RPC 契约缺陷修复
- 覆盖 finding：C-04 / C-05 / C-06 / C-07（本卡合同）+ C-10 / C-11 / C-12（W14 追加归属，同落 `cli/main.py`）
- `allowed_paths`：`cli/main.py`、`tests/`、`deliverables/software-company/`
- 执行模式：daemon HTTP authority（`RpcDBProxy` → `route_rpc`），Python 仅 thin client + 编排
- 时间：2026-09-13

---

## 1. 代码变更（落点逐条）

| finding | 落点 | 修法 |
|---|---|---|
| C-04 | [cli/main.py](../../cli/main.py) `_handle_churn` trend 循环 | `for t in trend[:20]` → `for item in trend[:20]`（消除对模块级 i18n 函数 `t` 的遮蔽） |
| C-05 | [cli/main.py](../../cli/main.py) `_handle_test_impact` 测试列表循环 | `for i, t in enumerate(tests, 1)` → `for i, item in enumerate(tests, 1)` |
| C-06 | [cli/main.py](../../cli/main.py) `RpcDBProxy._METHOD_MAP` / `_handle_assignment` | `create_assignment` 参数名对齐 daemon 扁平 Identity（`task_id, role, agent_id, session_id, model_id`）；新增 `_unwrap_bool_result`（dict/2 元组归一）与 `_resolve_assignment_workspace_instance_id`（task-scoped 请求显式补 `workspace_instance_id`）；两处调用点改用归一解包 |
| C-07 | [cli/main.py](../../cli/main.py) `_METHOD_MAP` | 新增 `get_semgrep_summary: ("get_semgrep_summary", "READ_ONLY", ("target_paths",))`，显式路径不再降级为 `arg0` 被静默丢弃 |
| C-10 | [cli/main.py](../../cli/main.py) `_handle_rule_list` | 兼容 MCP-061 信封 `{"rules": […], "count": n}`（非 dict 时按裸 list 回退） |
| C-11 | [cli/main.py](../../cli/main.py) `_agent_start` | `UnixDaemonRpcClient(socket_path=get_default_daemon_endpoint())` → `HttpDaemonRpcClient()` |
| C-12 | [cli/main.py](../../cli/main.py) `_agent_status` | 同上 → `HttpDaemonRpcClient()` |

测试侧（step#4）：[tests/test_cli_061_http_rpc.py](../../tests/test_cli_061_http_rpc.py) 新增
`test_cli061_semgrep_quick_target_paths_transmitted`（C-07 回归守卫：断言具名 `target_paths`
透传且 `arg0` 不出现）。

`git diff --check`：clean（RC=0）。

---

## 2. 命令实跑回执（前 / 后，逐字）

### 2.1 前（`git stash push -- cli/main.py` 还原至 HEAD 后实测）

```
### BEFORE-1 cw churn
✗ Subcommand 'churn' failed: cannot access local variable 't' where it is not associated with a value
RC=0
### BEFORE-2 cw rule list
=== Active Rules (2) ===
✗ Subcommand 'rule' failed: string indices must be integers, not 'str'
RC=0
### BEFORE-3 cw assignment create
✗ Subcommand 'assignment' failed: invalid_params: 缺少字段: workspace_instance_id
RC=2
### BEFORE-4 cw assignment revoke
✗ Subcommand 'assignment' failed: invalid_params: 缺少字段: task_id
RC=2
```

- BEFORE-2 的 `Active Rules (2)` 是**缺陷产物**：信封 dict 有 `rules`/`count` 两个键，
  修复前 `len(信封)`=2；修复后实际 active rules=0（见 AFTER-2）。
- BEFORE-4 与 AFTER-4 **逐字一致**（revoke 未因本卡变更而改变，见 §4 残余 C-15）。

### 2.2 后（同一工作树，本卡修复生效）

```
### AFTER-1 cw churn（exit 0）
=== Code Churn Analysis ===
  Time window: 90d
  Changed files: 386
  Current total lines: 606859
  Total churned lines: 368393
  Churn rate: 60.70%
### AFTER-1b cw churn [trend]
  [Churn Trend] (18 points):
    2026-07-16  ██████████████████████████████ 556 lines
    2026-07-17  ██████████████████████████████ 7752 lines
    2026-07-18  ██████████████████████████████ 9873 lines
    2026-07-20  ██████████████████████████████ 6440 lines
    2026-07-21  ██████████████████████████████ 6081 lines
### AFTER-2 cw rule list（exit 0）
=== Active Rules (0) ===
(empty)
### AFTER-3 cw assignment create（RC=2，见 §4 残余 C-14）
✗ Subcommand 'assignment' failed: internal_error: assignment_create: NOT NULL constraint failed: task_assignments.assignment_id
### AFTER-4 cw assignment revoke（RC=2，见 §4 残余 C-15）
✗ Subcommand 'assignment' failed: invalid_params: 缺少字段: task_id
```

### 2.3 semgrep（C-07）

```
$ cw semgrep stats
Semgrep findings statistics:
  Total findings: 0
（exit 0）

$ cw semgrep scan cli --quick
Semgrep multi-language analysis:
  Rules config: p/default
  Timeout: 180s
✗ Subcommand 'semgrep' failed: method_not_found: 未知方法: get_semgrep_summary
（RC=2）

$ cw semgrep scan cli
✗ Subcommand 'semgrep' failed: method_not_found: 未知方法: run_semgrep
（RC=2）
```

**诚实披露**：C-07 的 CLI 侧缺陷（位置参数降级 `arg0` 被静默丢弃）已修复，并由
`tests/test_cli_061_http_rpc.py` 的具名 `target_paths` 断言固化；但该命令
**端到端仍不可用**，根因是 daemon 侧 `semgrep_handlers.rs` 从未被编译/接线
（`rust_ext/src/daemon/mod.rs` 无 `pub mod semgrep_handlers;`），属 `rust_ext/**`
越界，已登记为残余 C-13（见 §4）。本卡未以任何本地旁路伪造 semgrep 成功。

### 2.4 test-impact（C-05）

```
$ cw test-impact _handle_assignment
=== Test Impact Analysis ===
  Target function: _handle_assignment
  Tests to run: 0
  (no related tests found, function may have no test coverage)
（exit 0）
```

**诚实披露**：当前工作区 test 关联表为空（`cw tests <QN>` 提示「未运行
`cw tests --build`」），故真实数据下 `Tests to run` 恒为 0，**未触发**循环体
（C-05 崩点在「命中 ≥1 测试」分支）。该分支由既有
[tests/test_cli_066_http_rpc.py](../../tests/test_cli_066_http_rpc.py)
（注入 2 条测试记录，断言 `test_foo` / `tests/test_a.py` 出现在输出）覆盖并已转绿。

---

## 3. 回归证据

```
$ C:\Python314\python.exe -m pytest tests/test_cli_005_http_rpc.py tests/test_cli_006_http_rpc.py \
    tests/test_cli_011_http_rpc.py tests/test_cli_020_http_rpc.py tests/test_cli_057_http_rpc.py \
    tests/test_cli_061_http_rpc.py tests/test_cli_066_http_rpc.py -q
.......................                                                  [100%]   (23 passed)
```

逐文件覆盖对照（step#4 检查项）：

| step#4 检查项 | 覆盖测试 | 结果 |
|---|---|---|
| trend 非空分支（C-04） | `test_cli_020_http_rpc.py`（2 点 trend，断言 `2026-08-08`） | pass |
| ≥1 测试命中分支（C-05） | `test_cli_066_http_rpc.py`（2 条测试，断言 `test_foo`/`tests/test_a.py`） | pass |
| assignment create/revoke 回包解析（C-06） | `test_cli_011_http_rpc.py`（dict 回包 4 例） | pass |
| C-07 target_paths 透传 | `test_cli_061_http_rpc.py::test_cli061_semgrep_quick_target_paths_transmitted`（新增） | pass |
| C-10 信封解包 | `test_cli_057_http_rpc.py`（3 例） | pass |
| C-11 / C-12 HTTP 迁移 | `test_cli_005_http_rpc.py`（3 例）/ `test_cli_006_http_rpc.py`（5 例） | pass |

---

## 4. 残余 finding（本卡执行中新发现，落点在 `rust_ext/**`，越界不改，仅如实登记）

| ID | 严重度 | 根因 | 落点 | 实跑回执 |
|---|---|---|---|---|
| C-13 | P1 | `semgrep_handlers.rs`（CLI-061 复刻 `run_semgrep` / `run_semgrep_and_save` / `scan_semgrep_incremental` / `get_semgrep_summary`）**从未被编译**：`rust_ext/src/daemon/mod.rs` 无 `pub mod semgrep_handlers;`，dispatch/http_server 亦无对应 RPC 分支 → daemon 对上述方法一律 `method_not_found` | `rust_ext/src/daemon/mod.rs`（缺模块声明）、`rust_ext/src/daemon/semgrep_handlers.rs`（未接线） | `method_not_found: 未知方法: run_semgrep` / `未知方法: get_semgrep_summary`（§2.3） |
| C-14 | P1 | daemon `handle_assignment_create` 的 INSERT 未包含 `assignment_id` 列，而 schema 定义 `assignment_id TEXT NOT NULL UNIQUE` → 任何 create 必 `NOT NULL constraint failed` | `rust_ext/src/daemon/admin_handlers.rs:557-562`（对照 `db/schema.py:1626-1639`） | `internal_error: assignment_create: NOT NULL constraint failed: task_assignments.assignment_id`（AFTER-3） |
| C-15 | P1 | daemon `handle_assignment_revoke` 强制 `task_id`(+可选 `role`)，与已文档化契约 `assignment_revoke(assignment_id)` 及 CLI `cw assignment revoke <assignment-id>` 不一致；且无 `assignment_id → task_id` 反查 RPC，CLI 侧无法兼容 | `rust_ext/src/daemon/admin_handlers.rs:577-600`（对照 `docs/mcp_tools.md:1972`、`docs/cli_reference.md:3246-3257`） | `invalid_params: 缺少字段: task_id`（BEFORE-4 == AFTER-4） |

**影响定界**：`cw assignment create|revoke` 的验收「在真实数据下不再崩」在本卡 `allowed_paths`
内**无法闭合**——本卡已完成全部 CLI 侧缺陷修复（解包归一 + 方法名对齐 + task-scoped
workspace 上下文补齐），阻断点已前移至 daemon 侧 C-14/C-15。已按 C 桶纪律登记，
不伪造本地回退、不使用 SQLite 旁路。

---

## 5. 验收逐条结论

| 验收项 | 结论 | 依据 |
|---|---|---|
| `cw churn` 不再崩 | ✅ 达成 | AFTER-1/AFTER-1b（exit 0，18 点 trend）+ `test_cli_020` |
| `cw test-impact` 不再崩 | ✅ 达成（空命中分支实跑；命中分支由测试覆盖） | §2.4 + `test_cli_066` |
| `cw assignment create` 不再崩 | ⚠️ CLI 侧达成，端到端被 C-14 阻断 | AFTER-3 + §4 |
| `cw assignment revoke` 不再崩 | ⚠️ 被 C-15 阻断（CLI 侧无可兼容路径） | BEFORE-4 == AFTER-4 + §4 |
| `cw semgrep stats <PATH>` 尊重显式路径参数 | ⚠️ CLI 侧达成（具名 `target_paths`），端到端被 C-13 阻断；`cw semgrep stats`（无路径子命令）实跑 exit 0 | §2.3 + `test_cli_061` |
| `cw rule list`（追加 C-10） | ✅ 达成 | AFTER-2 + `test_cli_057` |
| `cw-agent start/status` HTTP 迁移（追加 C-11/C-12） | ✅ 达成 | `test_cli_005` / `test_cli_006`（8 例全绿） |
| `git diff --check` | ✅ clean | §1 |
