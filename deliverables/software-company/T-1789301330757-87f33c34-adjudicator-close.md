# W17 承接卡 adjudicator 关闭证据：T-1789301330757-87f33c34

- 结论：Adjudicator 接受独立 reviewer PASS verdict，apply + close 收口任务。
- Reviewer verdict：`V-23583814b61df9f3985a69e6`（blind_first_pass / pass / findings=0）
- 绑定 step：`S-1789301330761-882dc8e0`（step_index 5，最后一步）
- Snapshot：`52a28d6c4f56d16a`；workspace：1
- 证据 manifest：`deliverables/software-company/w17_collab_authority_migration_evidence.md`
  `sha256:bcad75d82fc90f437d1f45e7776f9215cac001c4cf3e6f99cfc475241fa37704`

## 1. verdict provenance 校验（adjudicator 独立只读复核）

- `task_verdict_events` 最新行：phase=`blind_first_pass`、overall=`pass`、
  step_id=`S-1789301330761-882dc8e0`、snapshot_id=`52a28d6c4f56d16a`、
  view_manifest_hash=`23f3f1c6f9c8ae0c153e7bae9399fd4bb55af7c2e2c0fb435fdc5657125107f5`、
  workspace_id=1，role_contract_hash=`sha256:da1fa135…`（reviewer lineage
  `rcl-T-1789301330757-87f33c34-reviewer` r1）。
- reviewer 盲审内容：变更落在 `cli/main.py::_handle_collab`（4 个治理写方法统一收敛到
  `route_rpc(..., 'GOVERNANCE_WRITE')`）；回归 7 passed、`git diff --check` exit 0、
  `_handle_collab` 内 `DaemonClient` 构造为 0、fail-closed 负例保留。
- 2 例 `tests/test_cli_011_http_rpc.py` assignment 路由失败经 baseline 提交 `a2d559e` 对照复现，
  属既有缺陷（已由 C-06 承接卡 `T-1789290073049-6442e268` 覆盖），非本次引入。

## 2. apply/close 依据

- `review.state=passed`（verdict `V-23583814b61df9f3985a69e6`，findings_count=0）；
- 不触碰 apply/close 门禁语义，仅按正常收口路径调用 `task.apply` + `task.close`；
- 收尾使用 **reviewer role lease**（`L-aee37b0255a563bd`，fencing_counter=4，raw token 仅内存使用、
  未落盘、用后立即 release），identity 为 adjudicator（agent_id/session_id/model_id 与 lease 行逐字一致）。
- 权威事件链：`task_events` 8733 `applied`（review→applied）、8734 `closed`（applied→closed）；
  `tasks.status=closed`，`applied_at=1789307248.55834`，`closed_at=1789307249.7608633`。

## 3. 结论

任务 `T-1789301330757-87f33c34`（W17：`cw collab` 治理写命令面迁移到 HTTP authority）的
6 个 executor step 全部 done 并经独立 reviewer 盲审通过，adjudicator 复核一致后关闭。

终态：`lifecycle_status=closed` / `workflow_status=completed` / `decision=COMPLETE` /
`blocking_conditions=[]`。
