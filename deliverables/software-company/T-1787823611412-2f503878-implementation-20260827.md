# T-1787823611412-2f503878 implementation evidence

- task_id: `T-1787823611412-2f503878`
- step_id: `S-1787823611424-300819e8`
- scope: Rust daemon `task.reconcile` mutation route
- implementation:
  - Recursively enumerates the requested task and descendants inside the daemon-owned Task DB.
  - Selects only legacy `review` tasks with pending, in-progress, or unresolved failed steps.
  - Requires an immutable task workspace binding joined to an authority capture and an exact
    `workspace_instance_id` match; unavailable or mismatched targets are returned as skipped.
  - Applies only the deterministic `review -> in_progress` lifecycle repair in one transaction,
    appending an auditable event and action identity while preserving task steps and historical
    evidence.
  - Uses the protected mutation route and operation ledger for idempotent replay.
- files:
  - `rust_ext/src/daemon/task_collab.rs`
  - `rust_ext/src/daemon/dispatch.rs`
  - `rust_ext/src/daemon/task_loop/operation_store.rs`
- verification:
  - `tokenslim run cargo check --manifest-path rust_ext/Cargo.toml --no-default-features` — passed.
  - `git diff --check -- rust_ext/src/daemon/task_collab.rs rust_ext/src/daemon/dispatch.rs rust_ext/src/daemon/task_loop/operation_store.rs` — passed.
- safety boundary: no direct client-side SQLite mutation, no task-step deletion/update, no
  contract/binding/verdict fabrication, and no apply/close operation was performed.
