# Debian package control file for callwarden-enterprise (meta-package)
# Task: T-1783983162956-b2a8
# 元包 = daemon + agent + client（设计 §8.1）
# __VERSION__ / __ARCH__ 由 build_packages.sh 从 release/version.toml 注入

Package: callwarden-enterprise
Version: __VERSION__
Section: devel
Priority: optional
Architecture: __ARCH__
Maintainer: Call Warden Team <team@callwarden.dev>
Depends: callwarden-daemon (= __VERSION__), callwarden-agent (= __VERSION__), callwarden-client (= __VERSION__)
Description: Call Warden Enterprise (daemon + agent + client meta-package)
 Meta-package installing the full enterprise stack:
  - callwarden-daemon: system service, CAS, schema migration, backup
  - callwarden-agent: per-UID agent with systemd user unit
  - callwarden-client: cw-client + MCP proxy
 .
 This package contains no files; it only declares dependencies.
