# PyInstaller 自包含发布包的共享运行时白名单（core）。
# 不要改为安装 requirements.txt 或 wheel[all]：它们包含外置/可选能力，
# 会把 torch、sentence-transformers、sqlite-vec 和 semgrep 带入冻结环境。
#
# 本文件只包含 local 与 client/agent 两个 bundle 都需要的 core 依赖。
# 角色专属依赖见：
#   - requirements-runtime-local.txt   local bundle（追加 numpy；P1-G 后无 parser/grammar）
#   - requirements-runtime-client.txt  client/agent bundle（无 parser，仅 core）
mcp[rich]==1.28.1
pathspec==1.1.1
watchdog==6.0.0
requests==2.32.5
psutil==7.2.2
