# local bundle 运行时白名单：core + Rust parser 运行依赖。
# P1-G（2026-07-25）：Rust-only parser 生产切换完成，Python tree-sitter 核心
# 与 16 种 grammar wheel 已从正式发布包移除。生产解析由 Rust callwarden_core
# 独立完成，失败显式 fail closed，不再有 Python fallback（设计：
# rust-only-parser-cutover-plan.md §8 Phase 5）。
# client/agent bundle 严禁引用本文件（无本地解析需求）。
# Python parser 保留在源码仓库作为开发 reference，通过 pyproject.toml 的
# `parser-reference` extra 安装，不进入冻结包。
-r requirements-runtime.txt

# numpy 被 db_vector / Rust 余弦相似度辅助路径使用，仅 local bundle 需要。
numpy==2.4.3
