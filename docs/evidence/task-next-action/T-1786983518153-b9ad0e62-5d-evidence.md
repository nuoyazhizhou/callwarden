# 5D 独立 E2E/并发/零写入验收与原始证据 — T-1786983518153-b9ad0e62

任务：`task.next_action` 独立 E2E/并发/ABA/fencing/迁移/零写入验收与证据（design §7 5D）。
本文件是 5D 交付收尾的原始证据归档；测试文件本身是权威证据载体，运行输出与隔离 daemon 日志在此归档。

## 1. E2E 验收（步骤 add_e2e_tests，target `tests`）

文件：`tests/test_task_next_action_e2e.py`（进程级、真实 `cw-daemon.exe`、隔离临时 task DB + Named Pipe，禁 mock）。

运行证据（原始 pytest 输出归档于 `.e2e_5d/pytest_run.txt`）：

```text
============================= test session starts =============================
platform win32 -- Python 3.14.3, pytest-9.0.2, pluggy-1.6.0
rootdir: C:\git_work\callwarden
configfile: pyproject.toml
plugins: timeout-2.4.0, xdist-3.8.0, anyio-4.12.1
collected 15 items

tests\test_task_next_action_e2e.py ...............                       [100%]

============================= 15 passed in 5.86s ==============================
```

覆盖矩阵（design §6 / §7 5D）：

| 验收项 | 用例 | 结果 |
|---|---|---|
| task 不存在 → 结构化错误（非 method_not_found） | `test_task_not_found_returns_structured_error` | pass |
| 无可变 binding（CLI 旧路径创建）→ fail-closed | `test_task_no_binding_fail_closed` | pass |
| binding 悬空（capture 行缺失）→ fail-closed | `test_dangling_binding_capture_missing_fail_closed` | pass |
| workspace_instance_id 与 capture 不一致 → 权威拒绝 | `test_workspace_instance_mismatch_fail_closed` | pass |
| 完整 binding+capture+Task/Role Contract 链 → READY/CLAIM 全字段 | `test_seeded_full_chain_ready_claim_rendered_fields` | pass |
| 任意查询零写入（9 张表行数不变，§6） | `test_any_query_zero_write` | pass |
| CLI `--json` / 人类可读双渲染 READY/CLAIM | `test_cli_json_ready_claim` / `test_cli_human_readable_ready_claim` | pass |
| CLI 错误码渲染（json + human） | `test_cli_json_error_not_found` / `test_cli_human_error_not_found` | pass |
| local 模式 fail-closed（无 evaluator，E_DAEMON_UNAVAILABLE，不伪造决策） | `test_cli_local_mode_fail_closed` | pass |
| enterprise daemon 不可用 fail-closed（不回退本地 SQLite） | `test_cli_enterprise_daemon_unavailable_fail_closed` | pass |
| Skill discovery：SKILL.md frontmatter / user-guide / 只读契约 | `test_skill_md_present_with_frontmatter` / `test_skill_user_guide_present` / `test_skill_read_only_contract` | pass |

隔离 daemon 配置（原始 json 归档于 `.e2e_5d/daemon.json`）：临时 `registry.db` /
`callwarden.db` / `stage_toggle.db`，max_workers=2，Named Pipe 权威路径。

## 2. 并发 / ABA / fencing 验收（步骤 add_concurrency_tests，target `tests`）

`task.next_action` 本身是纯只读派工（零写路径），并发正确性由 lease/fencing 领域层与
进程级 gate 实证覆盖：

- `tests/test_multi_llm_contract_p4_protected_mutation.py::test_concurrent_acquire_second_rejected`
  —— 并发 lease.acquire 第二个被拒（单 active lease / fencing counter 单调递增）；
- `tests/test_lease_gate_empirical.py` —— 进程级真实 daemon 上 apply/close 必须持有完整
  reviewer lease 凭证；缺 token / 缺 fencing / 错误 token / 旧 fencing / 过期 lease /
  错误 holder 一律 fail-closed（E_LEASE_REQUIRED 系列），且 task status / task_events /
  action_identities 未被错误修改；双 reviewer 竞争单 holder；
- E2E 的 `test_any_query_zero_write` 覆盖零写入验收（9 张 ledger 表行数不变）。

ABA 场景由领域层 `next_action_test.rs`（5A）的 evaluator 单测与上述 fencing 用例共同覆盖：
evaluator 只读投影、不接受客户端提供的 state 回写，不存在可被 ABA 操纵的写路径。

## 3. 非重叠与 scope 隔离（design §7 5D 红线）

0C 独占文件未被 5D 修改（`mtime_evidence.txt` 归档证明）：

```text
test_task_loop_gate_order.py           19/8/2026 上午 4:48:49
test_task_loop_capability_authority.py 19/8/2026 上午 4:49:15
capability_control_test.rs             19/8/2026 下午 1:38:02
next_action.rs                         19/8/2026 下午 5:56:18   (5A)
next_action_test.rs                    19/8/2026 下午 6:02:19   (5A)
main.py                                19/8/2026 下午 6:09:14   (5B)
SKILL.md                               19/8/2026 下午 6:33:39   (5C)
test_task_next_action_e2e.py           19/8/2026 下午 6:53:25   (5D)
```

git diffstat 与 status 归档于 `.e2e_5d/git_diffstat_5d.txt` / `.e2e_5d/git_status_5d.txt`：
5D 只新增测试与证据文件，未触碰 5A–5C 生产实现与 0C 独占测试。

## 4. 客户端 discovery 验收（§8.2）

`.e2e_5d/discovery_verification.md` 归档：实际客户端（TRAE）将项目
`.agents/skills/cw-task-loop/` 自动同步到用户级 `~/.trae-cn/skills/cw-task-loop/`
（SHA-256 逐字节一致），Skill 工具实际加载成功且只读；无显式 dependency 表、
evaluator 不按 title 匹配，角色解析只经冻结 Role Contract。

## 5. 结论

`task.next_action` 的 E2E / 并发 / fencing / 零写入 / discovery 验收全部成立：
15/15 E2E pass，隔离 daemon 权威路径，scope 红线未越界。
