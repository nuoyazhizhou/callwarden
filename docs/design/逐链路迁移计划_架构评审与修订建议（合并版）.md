# 逐链路迁移计划 · 架构评审与修订建议书（合并版）

> 用途：交 **executor / planner** agent 据此修订 `Callwarden：逐 MCP 工具 _ 逐 CLI 链路 Rust daemon 迁移任务清单（草案）.md`
> 状态：全部关键决策已与用户确认；本建议书供 executor/planner 落实文档修订并建卡

---

## 0. 背景与已核验事实

- **目标**：把 70 个 `python_compat` MCP 工具 + 3 个 CLI 控制面链路，按「一链路一子任务」从 Python 迁移到 Rust daemon（只读 handler），消除粗粒度任务（S2/S3）含糊、执行者易偏离的问题。
- **核验**（活跃库 `~/.callwarden/callwarden.db`）：
  - 矩阵 `tool_migration_matrix.json` 239 条 = 129 rust_native + **70 python_compat** + 40 task_rpc，70 与草案一致。
  - 参考模块 `query_compat_handlers.rs`、`dispatch.rs`、`http_server.rs`、`compat_registry.py` 均存在。
  - 祖父 Epic `T-1787203926824-9f873bfc` 状态 **open**。
  - 其下：`S1 原`(review)、`S1-followup`(closed)、`S2-original`(open)、`S2-rebuilt`(open，描述=79 工具迁 Rust handler，已完成 9/79、白名单 71)、`S3`(open，PyO3/db 下线)。

---

## 1. 评审核心结论：P0 ownership 冲突（已定解决方向）

- **问题**：`S2-rebuilt`(open) 的 scope 与草案 70 工具迁移完全相同，且共享热文件（`dispatch.rs` / `http_server.rs` / `compat_registry.py` / 矩阵）。直接建 73 张卡会与 open 的 S2 形成**双 ownership**，违反 AGENTS.md「避免重叠创建；ownership 相交时拆分或串行」。
- **解决（用户确认）**：关闭粗粒度任务，改逐链路分解。S2 已完成 9/79 已并入矩阵 129 rust_native，剩余 70 = MCP-001~070，**无重叠**。
- **关闭方式**：必须 **supersede（带 provenance verdict）**，不是删除；verdict 写明「79 工具迁移 re-decompose 为逐链路任务；9/79 已并入矩阵；剩余 70 = MCP-001~070；superseded by <新父任务>」。

---

## 2. 已确认的两项决策

1. **父任务挂载**：在祖父 Epic 下**新建**迁移父任务（如「S4 逐 MCP 工具 / CLI 链路迁移 Rust daemon（滚动窗口）」），**不 reopen** `S1-followup`。closed 审计记录不动，scope 自文档化。
2. **Retirement**：**保留 S3**（`PyO3 直调清理 + db/ 目录下线`）作 retirement Epic。草案 §6.6 的「另立独立 retirement 任务」改为**指向 S3**，不再新建。

---

## 3. 过程模型：A′ 流水线（取代严格 A / 拒绝 C）

**问题**：无常驻驱动时「review 通过才建下一个」让执行者空转，效率低。
**方案 A′（流水线）**：
- 执行者完成 Task N → 推进 `review` → **立即创建 Task N+1 并执行**（不等 review）。
- 独立 **reviewer agent** 异步消费 `review` 队列：PASS → 交 **adjudicator** apply/close；BLOCKED → 入 Blocked 队列。
- 一轮执行完后，执行者**批量对 Blocked 任务 bugfix** → 重新 review。

**为什么安全**：单执行者独占热文件（串行化未破）；滚动窗口仍在（不预建 73）。

**A′ 必须补的 4 约束**：
1. **管道深度浅**（1–2 个在飞，不要无界）。
2. **门禁先 apply**：以下模块首任务必须先 `applied`，才允许创建同模块后继（防 BLOCK 级联）：
   `MCP-001, 005, 010, 015, 016, 024, 029, 048, 054, 063`
   （对应 §4.1–4.9 各新 `.rs` 模块首项 + `query_compat_handlers` 首个新工具 + security/lsp 两模块首项）。
3. **职责分离**：reviewer 只给 `PASS/BLOCKED` verdict；`apply/close` 由独立 **adjudicator（持 reviewer lease）** 执行，reviewer 不可自判自批。
4. **门禁 BLOCK 立即停建下游**：若 MCP-001 / CLI-01 等地基任务 BLOCK，立刻停建后续，不再基于坏地基生产。

**C（一次预建 73）明确拒绝**：治理噪声、树堆积、冲突固化。

---

## 4. 草案需同步修订的条目（精确到节）

- **§1.1 / §7**：把「reopen S1-followup 作迁移父任务」替换为「**新建 Epic 子任务**；S2-original / S2-rebuilt 经 **supersede** 收口」。
- **§6.6**：retirement 改**指向保留的 S3**，删「另立独立 retirement 任务」。
- **§2 / §5 / 矩阵**：新增 **`port_type`** 列（`sql-projection` / `algorithm-port` / `external-wrapper` / `llm-job`）。算法移植需 golden parity 测试；外部/LLM 依赖须在**开卡前** fail-closed 到 job 设计（草案已标 039/047，应推广到全部）。
- **新增「过程模型 A′」节**：描述流水线、reviewer/adjudicator 分离、门禁清单与停建规则（替代原「Reviewer PASS 后才建下一个」表述）。
- **§6 滚动窗口**：明确「执行者推进 `review` 即建下一个」，并列出门禁清单（见 §3 约束 2）。

---

## 5. 有序写入序列

- **Phase 0 — 收口（走 reviewer→adjudicator 治理 close 路径）**：supersede 关闭 `S2-original` + `S2-rebuilt`（verdict 记 9/79 已并入矩阵）；`S3` 保持 open 作 retirement Epic。
- **Phase 1 — 建父**：在祖父 Epic 下新建迁移父任务（open），描述引用被 supersede 的 S2 与「全量下沉」scope。
- **Phase 2 — 滚动建卡（A′）**：CLI-01 → `review` → 建 CLI-02 → … → CLI-03 → MCP-001（门禁，applied 后再建同模块）→ … → MCP-070。模块边界（004/009/014/015/023/028/047/062/070）由 reviewer 复核 compat 注册仅剩未迁工具。全部完成后由 **S3** 接管 retirement。

---

## 6. 给 executor/planner 的执行清单（checklist）

- [ ] 修订草案文档：落实 §1.1/§7（新建父 + supersede）、§6.6（S3）、`port_type`、A′ 过程模型、门禁清单。
- [ ] **Phase 0**：对 `S2-original` / `S2-rebuilt` 走 review→adjudicator 关闭，verdict 写清 re-decompose 与 9/79 证据。
- [ ] **Phase 1**：新建迁移父任务（open），挂在祖父 Epic 下。
- [ ] **Phase 2**：按 A′ 滚动建卡；门禁任务 applied 前不建同模块后继；reviewer/adjudicator 分离；门禁 BLOCK 停建。
- [ ] 每卡仍遵循草案 §2 固定合同（Role Contract、5 类文件改动、证据、禁止项）。

---

## 7. 待澄清（未决，建卡前需确认）

- `S1 原`(T-1787203937193-0993d120) 仍在 **review**，其 scope（`cli/main.py → daemon RPC`）与 CLI-01（daemon health/manifest/capability 诊断）可能部分重叠。建 CLI-01 前请确认 S1-review 落点，或在 CLI-01 卡中显式划清边界，避免二次 ownership 相交。
